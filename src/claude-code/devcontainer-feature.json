{
    "id": "claude-code",
    "version": "1.0.0",
    "name": "Claude Code",
    "description": "Installs Claude Code CLI via the official native installer (https://claude.ai/install.sh)",
    "documentationURL": "https://github.com/kencyke/devcontainer-features/tree/main/src/claude-code",
    "keywords": ["claude", "claude-code", "ai", "coding-agent"],
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version to install. 'latest', 'stable', or a specific version number (e.g. '1.0.58')."
        },
        "requireIntegrityCheck": {
            "type": "boolean",
            "default": false,
            "description": "When true, fail the installation if the binary integrity check cannot be performed (e.g. manifest download failure). By default, latest/stable installs only warn on verification failure."
        },
        "forceReinstall": {
            "type": "boolean",
            "default": false,
            "description": "When true, always re-download and install Claude Code even if it is already present. Useful for ensuring the latest version when using channel installs (latest/stable)."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
